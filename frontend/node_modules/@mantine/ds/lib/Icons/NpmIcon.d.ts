import React from 'react';
interface NpmIconProps extends React.ComponentPropsWithoutRef<'svg'> {
    size?: number | string;
}
export declare function NpmIcon({ size, style, ...others }: NpmIconProps): React.JSX.Element;
export {};
