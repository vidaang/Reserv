import React from 'react';
import { BoxProps } from '@mantine/core';
interface HeaderControlsProps extends BoxProps {
    onSearch: () => void;
    githubLink: string;
    withDirectionToggle?: boolean;
}
export declare function HeaderControls({ onSearch, githubLink, withDirectionToggle, ...others }: HeaderControlsProps): React.JSX.Element;
export {};
